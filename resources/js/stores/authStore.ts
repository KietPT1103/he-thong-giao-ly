import {defineStore} from 'pinia'; import {login,logout,me,getCsrfCookie} from '../api/auth'; import type {User} from '../types/api';
export const useAuthStore=defineStore('auth',{state:()=>({user:null as User|null,isLoading:false,isSubmitting:false,initialized:false}),getters:{isAuthenticated:s=>!!s.user,roles:s=>s.user?.roles??[],permissions:s=>s.user?.permissions??[]},actions:{async initialize(){if(this.initialized)return;this.isLoading=true;try{this.user=(await me()).data.data;}catch{this.user=null;}finally{this.initialized=true;this.isLoading=false;}},async fetchCurrentUser(){this.initialized=false;await this.initialize();},async login(email:string,password:string){this.isSubmitting=true;try{await getCsrfCookie();this.user=(await login(email,password)).data.data;this.initialized=true;}finally{this.isSubmitting=false;}},async logout(){await logout();this.user=null;},hasRole(role:string){return this.roles.includes(role);},hasPermission(permission:string){return this.permissions.includes(permission);}}});
